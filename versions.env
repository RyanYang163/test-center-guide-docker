# ============================================================
# 应用与镜像参数 —— 打包与发版的主要改版入口
#
# ⚠️ 版本号要改【两处】：
#     本文件的 TOS_VERSION              —— 决定镜像 tag、Release tag
#     config.ini 的 "version"           —— 决定应用元数据里的版本
#   两处必须字符级一致，scripts/build-package.py 会校验并拦下不一致。
#   （docker-compose.yml 里的 @@VAR@@ 占位符由脚本自动替换。）
# ============================================================

# Docker Hub 命名空间（用户名）—— 已用 token 实测确认：whiteelm
#   （原写 whiteelm610，是别处的名字；实测 auth.docker.io 返回 401，改回 whiteelm 后 200）
# TOS 规范：镜像必须来自 Docker Hub，
# 且必须是「锁定了具体版本」的 tag，禁 :latest。
DOCKERHUB_USER=whiteelm

# 应用版本号：1-3 段数字、总长 ≤20、禁字母/连字符/前导零
TOS_VERSION=1.0.1

# 目标架构：x86_64 或 aarch64（每个架构单独出包、单独提交）
TOS_PLATFORM=x86_64

# ---- 基础镜像 ----
# 用 Docker Hub 官方 nginx:alpine（alpine 层最干净）。
#
# ⚠️ 这个 tag 是**会腐烂的**：镜像不在本地，CI 每次按 tag 重新拉，
#    而 alpine 基础包与 nginx 本身都在持续出 CVE 与修复版本。
#    2026-09-24 实测：`nginx:1.27-alpine`（2024 年的 minor）在 trivy 下
#    有 **40 个「存在官方修复」的 HIGH/CRITICAL** —— CI 门禁直接拦下、镜像不推。
#
# 换版本的方法：改本行 + Dockerfile 的 FROM（两处一起改），
# 然后**升版本号**重发（同一版本号的 Release 不允许覆盖）。
# 本机连不上 hub.docker.com 查不到标签 —— 扫描失败时 CI 会用 ::notice::
# 把线上较新的 alpine 标签回传（见 .github/workflows/release.yml 的扫描步骤）。
#
# 1.31.6 由 CI 回传的标签清单里选（线上最新；1.30.5 是它前面的稳定线）。
NGINX_IMAGE=nginx:1.31.6-alpine

# ---- 自建镜像（CI / scripts/build-image.sh 构建并推送）----
APP_IMAGE=${DOCKERHUB_USER}/test-center-guide:${TOS_VERSION}

# ---- 宿主机端口 ----
# 避开系统保留端口（22/80/443/445/3306/5050/5432/6379/8181/8443），
# 落在规范推荐的 8000-19999 区间。提交前在设备上确认没被占用。
WEB_PORT=19013
